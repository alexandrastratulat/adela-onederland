# Adela in ONEderland — Next.js scaffold

This commit initializes a Next.js + TypeScript + Tailwind scaffold for the project and adds a Loading scene component (Scene 1).

Run locally:

1. npm install
2. npm run dev

This is the incremental first step. Subsequent commits will add the three.js scenes, story scenes, GSAP transitions, and the rest of the project structure.
